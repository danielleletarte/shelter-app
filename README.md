### Running the app
- Start mongoDB from `/users` directory in terminal by running `mongod`
- In new termial window, connect to local dbs by running `mongo`
- Run `start` script within `package.json`
- Open GraphiQL at `http://localhost:4000/graphql`
